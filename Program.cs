Console.Write("Введите длину массива: ");
int N = int.Parse(Console.ReadLine()!);

string[] ArrSTR = 

string[] GetArray(int n)
{
    string[] res = new sting[n];

    for (int i = 0; i < n; i++)
    {
        Cosole.WriteLine("Введите " + (i+1) + " строку");
        res[i] = ;
    }
    return res;
}